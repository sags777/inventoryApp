import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  container: {
    maxWidth: "lg",
  },

  image: {
    width: "100%",
    maxWidth: 500,
  },
}));

export default useStyles;
